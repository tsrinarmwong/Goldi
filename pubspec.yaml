name: goldi
description: "Bill Splitter"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 < 4.0.0"

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  # google_ml_kit: ^0.18.0
  fl_chart: ^0.68.0
  share: ^2.0.4
  animated_text_kit: ^4.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
